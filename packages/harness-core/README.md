# `@anarkhgatsby/deepseek-harness-core`

DeepSeek Harness channel plugins share this package's message bus and gateway core.

Most users do not need to import it directly. Install it when using a channel plugin that depends on it:

```bash
dsh plugin --profile web add @anarkhgatsby/deepseek-harness-core
```

MIT License.
